name: build-caddy-l4-fixed

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        goarch: [amd64, arm64]

    steps:
      - id: meta
        run: |
          set -euo pipefail
          
          TAG="v2.10.1"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          
          L4_SHA="5b50eb6ed84c74cd3d4b15f854b93b8b6d71424c"
          echo "l4_sha=${L4_SHA}" >> "$GITHUB_OUTPUT"
          
          echo "xcaddy_sha=$(git ls-remote https://github.com/caddyserver/xcaddy.git HEAD | awk '{print $1}')" >> "$GITHUB_OUTPUT"

      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false

      - name: Build + pack
        env:
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          set -euo pipefail

          env -u GOOS -u GOARCH -u CGO_ENABLED go install \
            github.com/caddyserver/xcaddy/cmd/xcaddy@${{ steps.meta.outputs.xcaddy_sha }}
          export PATH="$(go env GOPATH)/bin:${PATH}"

          mkdir -p build
          BIN="caddy-l4-${GOOS}-${GOARCH}"

          echo "Building Caddy ${{ steps.meta.outputs.tag }} with L4 commit ${{ steps.meta.outputs.l4_sha }}..."

          xcaddy build "${{ steps.meta.outputs.tag }}" \
            --with github.com/mholt/caddy-l4@${{ steps.meta.outputs.l4_sha }} \
            --output "build/${BIN}"

          chmod +x "build/${BIN}"
          strip "build/${BIN}" 2>/dev/null || true
          tar -C build -czf "build/${BIN}-${{ steps.meta.outputs.tag }}.tar.gz" "${BIN}"

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: "caddy-l4 ${{ steps.meta.outputs.tag }} (Fixed)"
          prerelease: false
          overwrite_files: true
          files: build/*.tar.gz
          body: |
            Fixed version build:
            - caddy:    ${{ steps.meta.outputs.tag }}
            - caddy-l4: ${{ steps.meta.outputs.l4_sha }}
            - xcaddy:   ${{ steps.meta.outputs.xcaddy_sha }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
